# Анализ trace `Trace-20260320T101613.json`

## Выводы по trace (автоматический разбор JSON)

- **~259k** событий; **~3182** событий `ph:"X"` с `dur > 50000` (эвристика «длиннее ~50 ms» при `dur` в микросекундах как в предыдущем отчёте — при необходимости перепроверить масштаб `dur` для вашей версии Chrome).
- Самые длинные блоки совпадают с **`FireAnimationFrame`** + **`FunctionCall`** на **`index-*.js`** (минифицированное имя `Ot` — весь кадр в одном колбэке rAF).
- В цепочке рядом встречаются **`v8::Debugger::AsyncTaskRun`** / **`disabled-by-default-v8.inspector`**: типично при записи **Performance** с открытыми **DevTools** — даёт сильный оверхед и занижает FPS в записи.
- Много событий категории **GC** / **devtools.timeline** — возможен лишний шум от профилировщика.

**Рекомендация по измерениям:** для оценки «реального» FPS сделать короткую запись в **инкогнито без расширений**, по возможности **без открытой панели Sources/Debugger**, и смотреть на FPS в HUD игры.

## Изменение в коде (по статическому анализу + типичным узким местам)

В `simulation.js` на каждый кадр вызывался `updateInventoryUI()`, который делал **`innerHTML`** панели бустов → лишний **layout/style** каждый кадр. Исправление: обновлять DOM только при изменении сигнатуры (счётчики + отображаемые секунды бустов).

## Верификация по `debug-ba4c7a.log` (после сигнатуры панели)

- **H1:** `simMs` ~0.1–0.5 ms, **`drawMs` ~50–71 ms** — узкое место **рендер**, не симуляция.
- **H2:** события `boost panel innerHTML` только при смене `sig` (в т.ч. раз в секунду при тикающем таймере скорости), не 60/s.
- **Следы:** рост **`tracks` до 1000+** при высоком `drawMs` — после **lifetime** массив всё ещё мог быть огромным. Добавлен потолок **`MAX_TRACKS_IN_WORLD`** (512): лишние старые следы выбрасываются через `shift()`.

## Trace `Trace-20260320T102702.json`

- Длинные **`FunctionCall`** снова совпадают с **`loop`** (`gameClient.js`) и цепочкой **inspector/AsyncTaskRun** — при оценке FPS не записывать trace с открытым Performance или сравнивать с HUD без записи.
- Карта **3200×1800**: каждый кадр два **`fillRect`** с паттерном травы/перлина + сетка — тяжёлая работа. В **`world.js`** подложка (трава + перлин + сетка) собирается в **один offscreen** и далее на кадр выполняется один **`drawImage`** (пока не сменились размер/биом/готовность текстур).

## Логи `debug-ba4c7a` (runId `fps16-followup`)

- **`simMs`** ≈ 0.1–0.5 ms, **`drawMs`** ≈ 23–40 ms (пики ~51–60 ms) — узкое место рендер, не симуляция.
- **`landRebuild`: 0** на всех строках — кэш земли не пересобирается каждый кадр.
- **`dpr`: 1**, крупный буфер canvas (широкий монитор в полноэкране) — не гипер-масштаб DPR, а объём работы по кадру.
- Рост **`tracks`** сопровождает рост **`drawMs`**; при **`MAX_TRACKS_IN_WORLD`** = 512 дорого перебирать и выставлять матрицу на каждый след. **Отсечение следов вне видимой области** (`effects.drawTracks` + полуразмеры вида из `drawFrame`) снижает число реальных отрисовок без изменения симуляции.

### Верификация `post-view-cull` (лог `.cursor/debug-ba4c7a.log`)

- **`drawMs`** по-прежнему ~22–45 ms, **`fpsHud`** ~28–39 — без резкого сдвига относительно прогона `fps16-followup` (джиттер кадра и сцена отличаются).
- **Ошибка лимита:** в логе встречались **`tracks`: 552, 630** при лимите 512 — следы противника добавляются из **`messageHandlers`** через **`addTrack`** между кадрами, до хвоста **`runSimulation`**, где стоит `while (tracks.length > MAX_TRACKS_IN_WORLD)`. Исправление: сразу после **`push`** в **`addTrack`** выполнять тот же **`shift()`**, пока длина не станет ≤ лимита.

### Верификация `post-track-cap-inline`

- Во всех строках лога **`tracks` ≤ 512** (после насыщения стабильно **512**), превышений как до фикса нет — инструментализация снята.
